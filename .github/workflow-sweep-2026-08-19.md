# Workflow sweep — 2026-08-19

This temporary verification marker exists only to trigger the current push and pull-request workflow surfaces against the authoritative master state. It changes no runtime code, test denominator, threshold, gate, workflow semantics, or release claim.
